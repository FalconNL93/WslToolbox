<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <!-- Default StackPanel -->
    <Style TargetType="{x:Type StackPanel}">
        <Setter Property="Margin" Value="0,0,0,5" />
        <Style.Resources>
            <Style TargetType="{x:Type ui:Button}" BasedOn="{StaticResource {x:Type ui:Button}}">
                <Setter Property="Margin" Value="0,0,0,5" />
                <Setter Property="MinWidth" Value="120" />
                <Setter Property="Padding" Value="30,6" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
            </Style>

            <Style TargetType="ui:CardControl" BasedOn="{StaticResource {x:Type ui:CardControl}}">
                <Setter Property="Margin" Value="0,0,0,5" />
            </Style>
        </Style.Resources>
    </Style>

    <!-- Horizontal StackPanel -->
    <Style x:Key="Horizontal" TargetType="{x:Type StackPanel}" BasedOn="{StaticResource {x:Type StackPanel}}">
        <Style.Resources>
            <Style TargetType="{x:Type ui:Button}" BasedOn="{StaticResource {x:Type ui:Button}}">
                <Setter Property="Margin" Value="0,0,5,0" />
            </Style>
        </Style.Resources>
    </Style>
</ResourceDictionary>