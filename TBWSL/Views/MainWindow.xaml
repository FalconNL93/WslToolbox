<Window x:Class="WslToolbox.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="WSL Toolbox" Height="421" Width="626" WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize" WindowStyle="SingleBorderWindow" Icon="/Resources/Images/toolbox.png">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="63*" />
            <ColumnDefinition Width="250*" />
        </Grid.ColumnDefinitions>
        <Label Content="WSL Service" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" FontWeight="Bold" Height="26" Width="83" />
        <Label Content="Distribution" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" FontWeight="Bold" Height="26" Width="83" Grid.Column="1" />
        <ListView Margin="132,41,10,10" x:Name="DistroDetails" Grid.Column="1" SelectionChanged="DistroDetails_SelectionChanged" MouseDoubleClick="DistroShell_Click">
            <ListView.Resources>
                <Style TargetType="{x:Type GridViewColumnHeader}">
                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                </Style>
            </ListView.Resources>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="auto" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="State" Width="auto" DisplayMemberBinding="{Binding State}" />
                    <GridViewColumn Header="WSL Version" DisplayMemberBinding="{Binding Version}" />
                </GridView>
            </ListView.View>
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Start" Click="DistroStart_Click" IsEnabled="{Binding ElementName=DistroStart, Path=IsEnabled}"></MenuItem>
                    <MenuItem Header="Stop" Click="DistroStop_Click" IsEnabled="{Binding ElementName=DistroStop, Path=IsEnabled}"></MenuItem>
                    <MenuItem Header="Restart" Click="DistroRestart_Click" IsEnabled="{Binding ElementName=DistroRestart, Path=IsEnabled}"></MenuItem>
                    <MenuItem Header="Set default" Click="DistroSetDefault_Click" IsEnabled="{Binding ElementName=DistroSetDefault, Path=IsEnabled}"></MenuItem>
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>
        <Label x:Name="DefaultDistribution" Content="DefaultDistribution" Grid.Column="1" HorizontalAlignment="Left" Margin="192,10,0,0" VerticalAlignment="Top" Width="298" />
        <Label Content="Default:" HorizontalAlignment="Left" Margin="132,10,0,0" VerticalAlignment="Top" FontWeight="Bold" Height="26" Width="55" Grid.Column="1" />
        <TextBlock x:Name="StatusBlock" HorizontalAlignment="Center" Height="173" Margin="0,203,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="106" Text="StatusBlock" />
        <StackPanel HorizontalAlignment="Center" Height="162" Margin="0,41,0,0" VerticalAlignment="Top" Width="106">
            <StackPanel.Resources>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Margin" Value="0,5,0,0" />
                </Style>
            </StackPanel.Resources>
            <Button x:Name="StartWsl" Content="Start" HorizontalAlignment="Center" Height="25" VerticalAlignment="Top" Width="106" Click="StartWsl_Click" />
            <Button x:Name="StopWsl" Content="Stop" HorizontalAlignment="Center" Height="25" VerticalAlignment="Top" Width="106" Click="StopWsl_Click" />
            <Button x:Name="RestartWsl" Content="Restart" HorizontalAlignment="Center" Height="25" VerticalAlignment="Top" Width="106" Click="RestartWsl_Click" />
            <Button x:Name="UpdateWsl" Content="Update" HorizontalAlignment="Center" Height="25" VerticalAlignment="Top" Width="106" Click="UpdateWsl_Click" />
            <Button x:Name="RefreshWsl" Content="Refresh" HorizontalAlignment="Center" Height="25" VerticalAlignment="Top" Width="106" Click="RefreshWsl_Click" />
        </StackPanel>
        <StackPanel Grid.Column="1" HorizontalAlignment="Left" Height="335" Margin="10,41,0,0" VerticalAlignment="Top" Width="106">
            <StackPanel.Resources>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Margin" Value="0,5,0,0" />
                    <Setter Property="Width" Value="106" />
                </Style>
            </StackPanel.Resources>
            <Button x:Name="DistroStart" Content="Start" HorizontalAlignment="Left" Height="25" VerticalAlignment="Top" Click="DistroStart_Click" />
            <Button x:Name="DistroStop" Content="Stop" HorizontalAlignment="Left" Height="25" VerticalAlignment="Top" Click="DistroStop_Click" />
            <Button x:Name="DistroRestart" Content="Restart" HorizontalAlignment="Left" Height="25" VerticalAlignment="Top" Click="DistroRestart_Click" />
            <Button x:Name="DistroSetDefault" Content="Set default" HorizontalAlignment="Left" Height="25" VerticalAlignment="Top" Click="DistroSetDefault_Click" />
            <Button x:Name="DistroUninstall" Content="Uninstall" HorizontalAlignment="Left" Height="25" VerticalAlignment="Top" Click="DistroUninstall_Click" />
            <Button x:Name="DistroInstall" Content="Install" HorizontalAlignment="Left" Height="25" VerticalAlignment="Top" Click="DistroInstall_Click" />
            <Button x:Name="DistroConvert" Content="Convert" HorizontalAlignment="Left" Height="25" VerticalAlignment="Top" Click="DistroConvert_Click" />
            <Button x:Name="DistroShell" Content="Start shell" HorizontalAlignment="Left" Height="25" VerticalAlignment="Top" Click="DistroShell_Click" />
        </StackPanel>
    </Grid>
</Window>